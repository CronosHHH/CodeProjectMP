\doxysection{Class List}
Here are the classes, structs, unions and interfaces with brief descriptions\+:\begin{DoxyCompactList}
\item\contentsline{section}{\mbox{\hyperlink{classClustering}{Clustering}} \\*This class is used to obtain a clustering of a given set of locations (\doxylink{classLocation}{Location} objects). A clustering algorithm groups the input locations into K disjoint groups (clusters). The clustering algorithm used in this class is the K-\/\+Means algorithm. Each group will consist of a disjunct subset of the original location set and a representative location (centroid), which is calculated as the vector of the means of the locations in the group. The \doxylink{classClustering_a17977c6467960744c8bc71ffb97aa9fc}{run()} method in this class executes the algorithm. An example of use is the following\+: \doxylink{classVectorLocation}{Vector\+Location} locations; Suppose here code to fill the vector of locations \doxylink{classClustering}{Clustering} clustering; clustering.\+set(locations, 5); clustering.\+run(); }{\pageref{classClustering}}{}
\item\contentsline{section}{\mbox{\hyperlink{classLocation}{Location}} \\*It stores data about a location where people spend money on some service or product. A location contains data about the x and y position (double values) in the 2D space and the name of the location (a string that can contain several words) }{\pageref{classLocation}}{}
\item\contentsline{section}{\mbox{\hyperlink{classVectorInt}{Vector\+Int}} \\*A \doxylink{classVectorInt}{Vector\+Int} object contains a vector of integer elements. It has a capacity (maximun number of elements that can be stored in the vector) and a size (number of elements the vector currently contains) }{\pageref{classVectorInt}}{}
\item\contentsline{section}{\mbox{\hyperlink{classVectorLocation}{Vector\+Location}} \\*A \doxylink{classVectorLocation}{Vector\+Location} object contains a vector of \doxylink{classLocation}{Location} objects. It has a capacity (maximun number of Locations that can be stored in the vector) and a size (number of Locations the vector currently contains). The public methods of this class do not allow a \doxylink{classVectorLocation}{Vector\+Location} to contain two \doxylink{classLocation}{Location} objects with identical names. This class uses an array of \doxylink{classLocation}{Location} objects with a fixed capacity to store the set of locations }{\pageref{classVectorLocation}}{}
\end{DoxyCompactList}
